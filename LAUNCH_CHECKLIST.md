# 🚀 Чеклист запуска бота

## ✅ Что исправлено в коде:

1. **config.py** - Убрано дублирование импортов `load_dotenv()`
2. **services/cash_shift_report.py** - Заменён `bare except` на конкретные исключения с логгированием
3. **services/writeoff_documents.py** - Улучшена обработка ошибок при парсинге сумм
4. **handlers/departments_manager.py** - Все callback_data используют индексы (0-3) вместо названий цехов
5. **bot.py** - Все роутеры корректно зарегистрированы в правильном порядке

## 📋 Перед запуском проверьте:

### 1. Файл `.env` должен существовать и содержать:
```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
MODE=dev
IIKO_LOGIN=your_iiko_login
IIKO_PASSWORD=your_iiko_password
```

### 2. Установите зависимости:
```powershell
pip install -r requirements.txt
```

### 3. Проверьте доступность PostgreSQL:
- База данных создана
- Пользователь имеет права на создание таблиц
- Порт 5432 открыт

### 4. Проверьте доступность iiko API:
- API сервер доступен
- Логин и пароль верны
- Сеть позволяет подключение к iiko

## 🚀 Запуск:

### Локальный режим (polling):
```powershell
python main.py
```

### Production режим (webhook):
```powershell
python webhook.py
```

## 🔍 Что будет при запуске:

1. ✅ Инициализация пула подключений к БД
2. ✅ Создание таблиц (если не существуют):
   - `position_commissions` (комиссии по должностям)
   - `employee_position_history` (история должностей)
   - `settings` (настройки Яндекс комиссии)
   - `department_positions` (цеха и должности)
3. ✅ Предзагрузка складов
4. ✅ Запуск фонового мониторинга должностей (первая проверка через 1 час)
5. ✅ Удаление webhook (для polling режима)
6. ✅ Запуск polling/webhook

## 🎯 Функционал бота:

### Для администраторов (ADMIN_IDS в config.py):
- 📊 **Отчёты**:
  - Зарплата (ФОТ) за период
  - Выручка и себестоимость
  - Отчёт по смене кассы
  
- ⚙️ **Настройки**:
  - Комиссии по должностям
  - Комиссия Яндекс доставки
  - Корректировка должностей сотрудников
  - Управление цехами (добавление/удаление должностей)
  
- 📦 **Команды** (загрузка данных из iiko):
  - Сотрудники
  - Номенклатура
  - Группы
  - Склады
  - Справочники
  - Поставщики
  - Счета

### Для всех пользователей:
- 📝 **Создание документов**:
  - Акт списания
  - Расходная накладная (по шаблону или вручную)
  - Внутреннее перемещение
  - Создание расхода
  
- 📋 **Шаблоны**:
  - Создание шаблонов накладных
  - Использование готовых шаблонов

## ⚠️ Возможные проблемы и решения:

### Ошибка: "BOT_TOKEN not set"
**Решение:** Создайте файл `.env` и добавьте `BOT_TOKEN=...`

### Ошибка подключения к БД
**Решение:** 
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права пользователя БД

### Ошибка: "TelegramBadRequest: BUTTON_DATA_INVALID"
**Решение:** Уже исправлено - все callback_data используют короткие индексы

### Медленный запуск бота
**Решение:** Первая проверка должностей теперь через 1 час, а не сразу

### Ошибка: "Не удалось получить токен iiko"
**Решение:** 
- Проверьте `IIKO_LOGIN` и `IIKO_PASSWORD` в `.env`
- Убедитесь в доступности API сервера iiko

## 📊 Мониторинг:

После запуска смотрите логи:
- `✅` - успешные операции
- `⚠️` - предупреждения (не критично)
- `❌` - ошибки (требуют внимания)

## 🆘 Поддержка:

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте доступность внешних сервисов (БД, iiko)
4. Перезапустите бота
