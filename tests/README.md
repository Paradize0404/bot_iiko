# 🧪 Тесты для bot_iiko

## Структура тестов

```
tests/
├── __init__.py                 # Инициализация пакета
├── run_all_tests.py           # Главный скрипт запуска всех тестов
├── test_date_conversion.py    # Тесты конвертации форматов дат
├── test_salary_logic.py       # Тесты бизнес-логики зарплат
└── test_integration.py        # Интеграционные тесты
```

## Как запустить тесты

### Все тесты сразу:
```bash
python tests/run_all_tests.py
```

### Отдельные наборы:
```bash
# Тесты конвертации дат
python tests/test_date_conversion.py

# Тесты бизнес-логики
python tests/test_salary_logic.py

# Интеграционные тесты
python tests/test_integration.py
```

## Что покрывают тесты

### ✅ test_date_conversion.py (3 теста)
- Конвертация `DD.MM.YYYY` → `YYYY-MM-DD`
- Обработка невалидных форматов дат
- Расчёт количества дней в периоде

### ✅ test_salary_logic.py (9 тестов)
- Расчёт месячных ставок пропорционально дням
- Расчёт почасовой оплаты
- Расчёт бонусов от выручки
- Пересечение периодов (должность vs период расчёта)
- Маппинг должностей на цеха
- Расчёт комиссии Яндекса
- Расчёт итоговой выручки

### ✅ test_integration.py (7 тестов)
- **КРИТИЧЕСКИЙ**: Формат дат в API-запросах
- Формирование параметров API
- Структура данных зарплаты
- Агрегация по цехам
- Обработка ошибок (пустые данные, неизвестные должности)

## Результаты последнего запуска

```
✅ test_date_conversion.py:    3/3 PASSED
✅ test_salary_logic.py:       9/9 PASSED  
✅ test_integration.py:        7/7 PASSED

🎉 ИТОГО: 19/19 ТЕСТОВ ПРОШЛИ УСПЕШНО
```

## Зачем нужны тесты?

1. **Предотвращение регрессий** - изменения в коде не сломают работающую функциональность
2. **Документация** - тесты показывают, как должна работать система
3. **Быстрая отладка** - находим проблемы за секунды, а не часы
4. **Уверенность в деплое** - знаем, что система работает правильно

## Добавление новых тестов

При добавлении новой функциональности:
1. Создайте тест, который проверяет новую логику
2. Запустите `run_all_tests.py` чтобы убедиться, что ничего не сломалось
3. Коммитьте код только если все тесты зелёные ✅
