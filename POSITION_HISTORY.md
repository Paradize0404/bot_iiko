# Система отслеживания истории должностей

## Описание

Система автоматически отслеживает изменения должностей сотрудников и позволяет вручную корректировать историю с указанием дат. При расчете зарплаты учитываются разные настройки комиссий для разных периодов работы сотрудника.

## Основные компоненты

### 1. База данных истории должностей (`db/employee_position_history_db.py`)

**Таблица `employee_position_history`:**
- `id` - уникальный идентификатор записи
- `employee_id` - ID сотрудника из iiko
- `employee_name` - ФИО сотрудника
- `position_name` - название должности (полное, из справочника roles)
- `valid_from` - дата начала действия должности
- `valid_to` - дата окончания (NULL = текущая должность)

**Ключевые функции:**
- `get_current_position(employee_id, on_date)` - получить должность на конкретную дату
- `get_position_history_for_period(employee_id, from_date, to_date)` - получить все периоды должностей в диапазоне
- `set_employee_position(employee_id, employee_name, position_name, valid_from)` - установить должность с даты с автоматическим пересчетом периодов
- `update_position_from_iiko(employee_id, employee_name, new_position)` - обновить должность из iiko (только если изменилась)

### 2. Автоматический мониторинг (`services/position_monitor.py`)

**Функционал:**
- Запрашивает список всех сотрудников из iiko
- Сравнивает текущие должности с последними записями в БД
- Автоматически добавляет новых сотрудников
- Фиксирует изменения должностей с текущей датой
- Работает в фоновом режиме каждые 24 часа

**Функции:**
- `get_employees_with_positions_from_iiko()` - получить всех сотрудников с должностями из iiko
- `monitor_position_changes()` - проверить и зафиксировать изменения
- `run_periodic_monitoring(hours)` - запустить периодический мониторинг
- `run_once()` - запустить проверку один раз (для тестирования)

**Логирование:**
```
🆕 Новый сотрудник: Иванов Иван - Повар
🔄 Изменение должности: Петров Петр (Официант → Старший официант)
```

### 3. Ручная корректировка (`handlers/correct_position.py`)

**Команда:** `/correct_position` или кнопка "📝 Корректировка должности"

**Процесс:**
1. Выбор сотрудника из списка
2. Показ истории должностей за последние 90 дней
3. Ввод даты начала новой должности (DD.MM.YYYY)
4. Выбор новой должности из списка
5. Автоматический пересчет всех периодов

**Валидация:**
- Дата не может быть в будущем
- Формат даты должен быть DD.MM.YYYY

**Пример истории:**
```
📋 История должностей (Иванов Иван):

01.12.2024 - 15.12.2024: Официант
16.12.2024 - по н.в.: Старший официант
```

### 4. Интеграция с расчетом зарплаты (`services/salary_from_iiko.py`)

**Логика:**
1. Для каждого сотрудника получаем историю должностей за период расчета
2. Разделяем attendance по периодам должностей
3. Для каждого периода применяем настройки должности (payment_type, fixed_rate, commission_percent, commission_type)
4. Суммируем результаты всех периодов

**Пример расчета:**
```
Сотрудник работал:
- 01.12-15.12: Официант (почасовая 300₽/ч, комиссия 1% от продаж)
- 16.12-31.12: Старший официант (посменная 5000₽, комиссия 1.5% от продаж)

Период 1: 120 часов × 300₽ + 500000₽ × 1% = 36000₽ + 5000₽ = 41000₽
Период 2: 16 смен × 5000₽ + 800000₽ × 1.5% = 80000₽ + 12000₽ = 92000₽
Итого: 133000₽
```

## Алгоритм управления периодами

При установке должности с даты X (`set_employee_position`):

1. **Удаляются** все записи, которые начинаются >= X
2. **Обрезаются** записи, которые пересекаются с X (их valid_to устанавливается в X-1)
3. **Создается** новая запись с valid_from = X и valid_to = NULL

**Пример:**
```
Было:
01.12.2024 - 31.12.2024: Официант

Устанавливаем "Повар" с 15.12.2024:

Стало:
01.12.2024 - 14.12.2024: Официант
15.12.2024 - по н.в.: Повар
```

## Запуск и тестирование

### Автоматический запуск (при старте бота)
```bash
python main.py
```
Мониторинг запускается автоматически и работает каждые 24 часа.

### Ручной разовый запуск (для тестирования)
```bash
python test_position_monitor.py
```

### Проверка истории должности
В боте: "📊 Отчёты" → "📝 Корректировка должности" → выбрать сотрудника

## Настройки

### Интервал мониторинга
В `main.py`:
```python
asyncio.create_task(run_periodic_monitoring(24))  # 24 часа
```

### Период отображения истории
В `handlers/correct_position.py`:
```python
history_start = datetime.now().date() - timedelta(days=90)  # 90 дней
```

## Логи

Основные события логируются с префиксами:
- 🆕 - новый сотрудник добавлен
- 🔄 - должность изменена
- 📋 - информация о периоде должности
- ✅ - успешное завершение операции
- ⚠️ - предупреждение (не критично)
- ❌ - ошибка

## Миграция данных

При первом запуске:
1. Создается таблица `employee_position_history`
2. Мониторинг добавит всех текущих сотрудников с их должностями
3. История начнет накапливаться с момента запуска

## Возможные проблемы

### Сотрудник не появляется в списке
- Проверьте, что у него задана должность в iiko
- Проверьте, что сотрудник не удален в iiko

### История не отображается
- Убедитесь, что мониторинг запущен хотя бы один раз
- Проверьте логи на наличие ошибок подключения к iiko API

### Зарплата считается неправильно
- Проверьте настройки должности в "⚙️ Настройка комиссий"
- Проверьте историю должностей через корректировку
- Проверьте логи расчета зарплаты (уровень DEBUG)

## API iiko

Используются endpoints:
- `/resto/api/employees` - список сотрудников
- `/resto/api/employees/roles` - справочник должностей
- `/resto/api/employees/attendance` - рабочее время

Требуется авторизация через token (cookie key).
